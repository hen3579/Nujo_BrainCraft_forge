# 2026-07-10

## 剖视图默认按键 V → 句号键
- `ClientEventHandler.TOGGLE_SECTION_VIEW` 默认键从 `GLFW.GLFW_KEY_V` 改为 `GLFW.GLFW_KEY_PERIOD`（"."键）
- 编译通过

## 剖视图默认关闭 + HUD 隐藏
- `SectionViewCuller`: `manualOverride`(默认false=自动检测) → `manualEnable`(默认false=关闭)
  - 激活条件：`birdview && manualEnable && detectEnclosedSpace`
  - `toggleManualOverride()` → `toggleManualEnable()`, `isManuallyOverridden()` → `isManuallyEnabled()`
  - `reset()` 中 `manualEnable = false`
- `ClientEventHandler`: V键消息改为"已开启"/"已关闭"；HUD在 `!isManuallyEnabled()` 时直接return不显示
- 编译通过

## 修复按键绑定中文显示/出界
- 为缺失的 3 个按键绑定添加 zh_cn/en_us 翻译：
  - `key.nujobraincraft.toggle_first_person`
  - `key.nujobraincraft.toggle_third_person_back`
  - `key.nujobraincraft.toggle_third_person_front`
- 为分类名添加翻译 `nujobraincraft.general`
- 中文翻译更短，避免控件出界

## 修复鸟瞰模式倒三角形剔除缺口（重要）
- **根因**：`GameRenderer.renderLevel()` 在调用 `LevelRenderer.renderLevel()` 之前，先用**透视投影矩阵**调用 `prepareCullFrustum` 构建裁剪视锥体，而实际渲染用的是我们的正交矩阵
- 透视 frustum 近端窄、远端宽；正交 frustum 等宽。近端屏幕边缘的 chunk 被透视 frustum 错误剔除 → 形成倒三角形缺口
- **修复**：在 `OrthoViewMixin` 新增第三个注入点——`prepareCullFrustum` 的 `@ModifyVariable`，在鸟瞰模式下将裁剪用的投影矩阵也替换为正交矩阵
- 修改文件：`Mixins/OrthoViewMixin.java`（+17行），编译通过

## 修复 prepareCullFrustum Mixin 导致的程序冻结（关键）
- **根因**：`Frustum.offsetToFullyIncludeCameraCube` 内部 for 循环无限执行
  - 正交投影的 M^{-T} × (0,0,1,0) 将 viewVector 缩放到 ~500x
  - 循环每次移动 camera 2000+ 格，目标 AABB 永远无法被 frustum 包含
  - `intersectAab() != -2` 永远为真 → 无限循环
- **修复**：新增 `@Redirect` 注入 `setupRender` → `offsetToFullyIncludeCameraCube` 调用
  - 鸟瞰模式下直接返回 frustum（跳过 offset），正常模式下调用原方法
  - 鸟瞰相机离地 10+ 格，不需要 offsetToFullyIncludeCameraCube 来防止近处裁剪
- 修改文件：`Mixins/OrthoViewMixin.java`（改用 @Redirect 注入）
- 编译通过

## 视锥体修复 — 解决鸟瞰视角地形穿透 (20:05)
- **问题**: 鸟瞰视角下关闭剖视图，远处地形被截断露出天空/虚空
- **根因**: 正交投影 zoom=10 太小 → halfWidth≈17.8 格 → Frustum 的 right/left 面截断远处 chunk
- **参考 RoN 三层方案**:
  1. **FrustumMixin** — 鸟瞰时 `isVisible(AABB)` 短路返回 true，关闭视锥体剔除
  2. **zoom 默认值增大** — 10→30，min 3→10，max 25→60，默认覆盖约 6-7 chunk 宽
  3. **near=-3000 / far=+3000** — 近裁剪面推到相机后方 3000 格（RoN 做法）
- 文件变更:
  - 新建 `Mixins/FrustumMixin.java`
  - `mixins.nujobraincraft.json` — client 列表添加 FrustumMixin
  - `OrthoviewClientEvent.java` — ZOOM=30/10/60, NEAR=-3000, FAR=+3000
- 编译通过

## 审计鸟瞰模式渲染管线投影矩阵覆盖情况
- 全面排查 GameRenderer.renderLevel → LevelRenderer 全链路投影矩阵使用
- **已覆盖（三重注入）**：renderLevel 参数、prepareCullFrustum 参数、RenderSystem 全局投影 → 方块/天空/云/粒子/天气/世界边界/Forge AFTER_SKY~AFTER_WEATHER 全部正交
- **未覆盖但不影响**：
  1. Forge AFTER_LEVEL 事件 → GameRenderer 层 posestack 变量，Mixin 无法触及；我们不订阅此事件
  2. renderItemInHand → 正确行为，手部渲染需要透视，且鸟瞰模式下手部不可见
  3. VertexSorting.DISTANCE_TO_ORIGIN → 正交投影下 w=1，对半透明排序影响极小，参照 RoN 方案
- 结论：核心渲染路径全部正交，无需额外修改

## 修复鸟瞰模式下粒子朝向歪斜（重要）
- **根因**：`Camera.setAnglesInternal()` 只更新 `xRot`/`yRot`，不更新 `rotation` 四元数和 `forwards`/`up`/`left` 方向向量
  - 粒子公告牌 (`SingleQuadParticle.render()`) 通过 `Camera.rotation()` 获取朝向
  - `Camera.setup()` 阶段 rotation = 玩家视角 (pitch=0)
  - `ComputeCameraAngles` 事件覆盖 lookYaw/lookPitch (pitch≈45°)
  - `setAnglesInternal()` 只改 xRot/yRot，不改 rotation
  - 结果：公告牌用 pitch=0 朝向，相机用 pitch=45° 俯视 → 45° 偏差表现为粒子歪斜
  - 透视投影下因透视缩短效应不明显，正交投影下完全暴露
- **修复**：`CameraMixin` 新增 `@Inject(method="setAnglesInternal", at=@At("TAIL"), remap=false)`
  - 在 setAnglesInternal 尾部同步更新 rotation/forwards/up/left（复制 setRotation 逻辑）
  - `remap=false` 是因为 setAnglesInternal 是 Forge 注入方法，不在 SRG 映射中
- 修改文件：`Mixins/CameraMixin.java`（+新增 Shadow 字段 4 个 + @Inject），编译通过

## 实现鸟瞰模式相机旋转（[ 和 ] 键）
- 新增两个 KeyMapping：
  - `CAMERA_ROTATE_LEFT` → `GLFW_KEY_LEFT_BRACKET`（`[` 键，顺时针 +45°）
  - `CAMERA_ROTATE_RIGHT` → `GLFW_KEY_RIGHT_BRACKET`（`]` 键，逆时针 -45°）
- `BirdviewClientEvent.rotateCameraYaw(double delta)` → 修改 `fixedYaw` 并钳制在 [0, 360)
- 按键处理在 `handleStartPhase` 中：
  - 仅鸟瞰模式下 `consumeClick()`，非鸟瞰不拦截（`[`/`]` 原版无用途，不存在冲突）
  - 旋转后不旋转玩家实体朝向，仅改相机视角
- 注册：`NujoBraincraft.registerKeyMappings`
- 本地化：zh_cn.json "鸟瞰相机左转/右转"，en_us.json "Rotate Birdview Left/Right"
- 修改文件：`ClientEventHandler.java`（+9行）、`BirdviewClientEvent.java`（+12行）、`NujoBraincraft.java`（+2行）、`zh_cn.json`（+2行）、`en_us.json`（+2行）
- 编译通过

## 18:14 — Reign of Nether 技术分析

完成了对 SoLegendary/reignofnether 项目的深度源码分析(GitHub clone + 逐文件阅读)，输出 `reignofnether_技术分析.md`。覆盖四个核心系统：

1. **战争迷雾**: FogOfWarClientEvents 三层亮度(brightChunks/semiFrozen/frozenChunks) + FrozenChunk 方块伪装(saveFakeBlocks 替换建筑方块为 air/草方块/煤矿石等伪装方块，loadBlocks 恢复)；LevelRendererMixin 拦截 compileChunks 跳过 semiFrozenChunks；ModelBlockRendererMixin 着色；CompiledChunkMixin 过滤 BlockEntity；EntityShadowMixin 隐藏阴影
2. **3D A* 寻路**: GridAStar 20方向邻接+坠落通道(MAX_FALL_DROP=3)+攀爬通道(spider)；WalkabilityGrid 懒加载+增量 dirty chunk reclassify (Copy-on-Write, settle=4 tick, ceiling=40 tick)；多线程 PathfinderWorkerPool (WORKER_THREADS=CPU核数/2); PathNavigationMixin 禁用原版重算+宽体坠落提交
3. **世界平坦化**: 纯数据包方案——flat_dimensions 零化 continent/erosion/ridges density function，complex spline offset 压低 terrain 到 ~-0.53，sloped_cheese clamp 到 1.5 短路洞穴；overworldify 空 carver (probability=0.01 + replaceable=air)；7 个 biome JSON 覆盖 carvers:[]
4. **视锥体修复**: OrthoViewMixin 注入正交投影(near=-3000, far=3000 → 近平面在相机后方)；CameraMixin offset -20 格；FrustumMixin 短路 offsetToFullyIncludeCameraCube 防死循环；Infinity AABB 用 zoom 距离替代

## 18:40 — 实施 Feature 1 世界平坦化 + Feature 3 战争迷雾

### Feature 1: 世界平坦化（纯数据包）
- 新建 `data/nujobraincraft/worldgen/noise_settings/brain_world_flat.json` — 自定义 noise_settings
  - noise_router: continents/erosion/ridges/depth → 0, barrier/fluid/lava → 0, vein → 0
  - temperature/vegetation 保留 overworld 引用（维持生物群系变化）
  - initial_density → nujobraincraft:flat/initial_density (y_clamped_gradient + old_blended_noise × 0.03)
  - final_density → nujobraincraft:flat/final_density (= initial_density，跳过洞穴处理)
  - aquifers_enabled: false, ore_veins_enabled: false
  - surface_rule: 简化版 above_preliminary_surface → air
- 新建 density_function/flat/ 下 6 个 JSON：continents(0), erosion(0), ridges(0), depth(0), initial_density(y_clamped_gradient -64→204 + noise), final_density(ref initial)
- 覆盖 carver: data/minecraft/worldgen/configured_carver/{cave,canyon}.json → probability=0, yScale=0, radius=0
- 修改 brain_world.json: settings → nujobraincraft:brain_world_flat
- 10 个文件就位，零 Java 代码

### Feature 3: 战争迷雾（剧情驱动简化版）
- 新建 `fogofwar/FogOfWarData.java` — 三层亮度(BRIGHT_VISIBLE=1.0, BRIGHT_DARK=0.35, BRIGHT_UNEXPLORED=0.10)，ChunkPos asLong 键值集合，每 10 tick 更新，NBT 序列化持久化
- 新建 `fogofwar/FogOfWarEvents.java` — @Mod.EventBusSubscriber(client)
  - onClientTick → FogOfWarData.update() + auto-save
  - onRenderLivingPre (HIGHEST) → 取消非揭示区块实体的渲染
  - onWorldLoad/onWorldUnload → 加载/保存 NBT
  - resolveSavePath: 单人→saves/<world>/nujobraincraft_fog.nbt，多人→游戏根目录
- 新建 `Mixins/ModelBlockRendererMixin.java` — 注入 putQuadData:
  - @Inject HEAD 捕获 pPos → ThreadLocal<Float> fogBrightness
  - @ModifyArgs 拦截 putBulkData → args[2] brightnesses[] *= fogBrightness
  - require=0（容错）
- 修改 mixins.nujobraincraft.json: client 新增 ModelBlockRendererMixin
- 编译通过 (BUILD SUCCESSFUL, 0 errors)

## 18:50 — 主世界也实现世界平坦化
- 新建 `data/minecraft/worldgen/noise_settings/overworld.json`，引用 nujobraincraft:flat/ density functions
- 与 brain_world_flat 完全一致的 noise_router 和 surface_rule
- 加上已有的 minecraft 命名空间 carver 覆盖（cave/canyon → probability=0），主世界实现完整平坦化
- 编译通过

## 18:57 — 修复群系分布：恢复 continents/erosion/ridges/depth 原版引用
- 零化这些参数会破坏 multi-noise biome source → 生物群系全部坍缩
- brain_world_flat + overworld noise_settings 的 noise_router 改回 minecraft:overworld/ 引用
- initial_density/final_density 仍用 flatten，地形平坦但群系正常分布

## 18:58 — 修复 ModelBlockRendererMixin @Inject 签名错误
- 崩溃: Invalid descriptor on putQuadData @Inject — 只匹配了 3 参数，Mixin 0.8.5 要求完整 15 参数
- 补全 VertexConsumer, PoseStack.Pose, BakedQuad, float×4, int×5 参数

## 19:00 — 修复世界生成 JSON 错误
- 崩溃: Failed to load registries（创建世界时）
- final_density.json: minecraft:reference 在 1.20.1 不存在 → 删除该文件，noise_settings 直接引用 initial_density
- cave.json: 缺少 floor_level + y 格式错误 (minecraft:uniform 嵌套)
- canyon.json: 缺少 shape + y 格式错误
- 修复: cave 补 floor_level; canyon 补 shape; 两者 y 改平铺 min_inclusive/max_inclusive
- 编译+启动通过 (runClient 19s 无 crash)

## 19:30 — 地表露天矿脉 + 密林树木密度降低
- **地表矿脉** (纯数据包):
  - `nujobraincraft/worldgen/configured_feature/surface_{coal,iron,gold}_ore.json` — ore 类型, discard_chance=0 (地表可见), size 6/4/3
  - targets: stone_ore_replaceables + grass_block + dirt (替换地表方块)
  - `nujobraincraft/worldgen/placed_feature/surface_{coal,iron,gold}_ore.json` — count 4/3/2, y=62~80 uniform
  - `nujobraincraft/forge/biome_modifier/add_surface_ores.json` — forge:add_features 到 #minecraft:is_overworld, step=underground_ores
- **树木密度降低** (覆盖 minecraft 命名空间 placed_feature):
  - `dark_forest_vegetation.json` — count 16→5 (31%)
  - `trees_jungle.json` — weighted count 50/51→15/16 (30%)
  - `bamboo.json` — noise_to_count_ratio 160→48 (30%)
  - `bamboo_vegetation.json` — weighted count 30/31→10/11 (33%)
- 修复 3 个 worldgen JSON 格式错误:
  1. carver y 字段: 缺 type:"minecraft:uniform", 缺 VerticalAnchor 对象 → 复制原版完整格式, probability=0
  2. carver 缺 yScale/horizontal_radius_multiplier/vertical_radius_multiplier/floor_level/shape 等 FloatProvider 字段 → 补全原版格式
  3. initial_density old_blended_noise y_scale=0.0 超出 [0.001:1000.0] 范围 → 改为 0.001
  4. noise_router temperature/vegetation: 用了不存在的字符串引用 "minecraft:overworld/temperature" → 改为内联 shifted_noise 对象 (原版格式)
- 编译+启动通过, 世界生成正常, 剧情系统正常加载

## 地形自然化重构 (RoN 风格) (19:50)
- 问题: 之前简化方案导致地形不自然——surface_rule 仅 230 字符(无草地/泥土)、final_density 跳过管线(无洞穴)、线性梯度代替 spline
- 解决方案: 复制原版 overworld.json 完整内容到 brain_world_flat.json 和 overworld.json
- 只覆盖两个密度函数:
  - `minecraft:overworld/offset` → 常量 -0.5 (原版 60K spline, 控制高度偏移)
  - `minecraft:overworld/factor` → 常量 3.0 (原版 34K spline, 控制高度变化倍率)
- 恢复原版 carver (cave probability=0.15, canyon 原版配置)
- 启用 aquifers/ore_veins (原版默认)
- 保留: 完整 surface_rule (32K, 草地/泥土/沙子/石头/水), 完整 final_density 管线 (spaghetti_2d/noodle/pillars/entrances + blend_density + interpolated + squeeze)
- 编译通过
